name: bugfix-lab

on:
  workflow_dispatch: {}

jobs:
  nitroai-windows-build-report-no-error-text:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Run oracle
        shell: powershell
        # FIX round 1 (2026-09-21): points at v3, which sources the "clone"
        # step's command text from the FIXED publik guide (isMac-branched);
        # v2 (still on disk, unmodified) carries the pre-fix literal for
        # VERIFY lens A to toggle against by checking out this workflow file
        # at the fix commit's parent.
        run: pwsh -File scripts/bugfix-lab/nitroai-windows-build-report-no-error-text-v3.ps1
